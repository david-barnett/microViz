# Get phyloseq with counts if available

Get phyloseq with counts if available

## Usage

``` r
ps_counts(data, warn = TRUE)
```

## Arguments

- data:

  A phyloseq or psExtra object.

- warn:

  A boolean or "error" string to control warning or error behaviour
  (default: TRUE).

## Value

A phyloseq object with counts if available.
